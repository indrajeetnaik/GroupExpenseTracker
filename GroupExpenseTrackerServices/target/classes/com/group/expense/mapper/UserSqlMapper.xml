<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.expense.mapper.UserSqlMapper">

  <insert id="addUser" parameterType="com.group.expense.model.User">
    INSERT INTO USERS (username , password ,firstname , lastname , phonenumber, email)
    VALUES (#{user.userName} ,#{user.password}, #{user.firstName} , #{user.lastName} , #{user.phoneNumber}, #{user.emailId});
  </insert>
  
  <select id="getAllUsers" resultType="com.group.expense.model.User">
      SELECT userName "userName", firstName "firstName", lastName "lastName", email "emailId" , phoneNumber FROM users; 
  </select>

</mapper>